SELECT PROJECTS.PROJECT_TITLE, PROJECTS.PROJECT_ID, sum(DEVELOPERS.SALARY), avg(DEVELOPERS.SALARY) FROM PROJECTS INNER JOIN DEVELOPERS
    ON PROJECT_ID=DEVELOPERS.PROJECT GROUP BY PROJECTS.PROJECT_ID ORDER BY avg(DEVELOPERS.SALARY);